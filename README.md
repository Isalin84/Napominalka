# Напоминалка

Небольшое desktop-приложение для тех, кто забывает сделать пару глотков воды, размяться или отойти от экрана. Вместо сухого уведомления в углу экрана появляется выбранный питомец. Он остаётся рядом, пока вы не подтвердите, что пауза действительно сделана.

Винни гавкает, Макс мурлычет, Совушка ухает, а Белочка щёлкает орешком. Звук можно выключить или сделать тише.

## Скачать

Готовые приложения лежат в [Releases](https://github.com/Isalin84/Napominalka/releases/latest).

| Система | Что скачать |
| --- | --- |
| macOS с Apple Silicon | `Napominalka-…-arm64.dmg` |
| macOS с Intel | `Napominalka-…-x64.dmg` |
| Windows | `Napominalka-…-setup.exe` |
| Windows без установки | `Napominalka-…-portable.exe` |

### Первый запуск на macOS

Сборка подписана ad-hoc, но не заверена в Apple: сертификат Developer ID стоит 99 долларов в год, и пока его нет. Поэтому при первом запуске macOS предупредит, что не может проверить разработчика. Приложение при этом рабочее, нужно один раз разрешить его запуск.

На macOS 15 и новее Apple убрала обход через правую кнопку. Порядок такой:

1. Перетащите приложение из образа в «Программы».
2. Запустите его. Появится предупреждение — нажмите «Готово».
3. Откройте «Системные настройки» → «Конфиденциальность и безопасность».
4. Пролистайте вниз. Там будет строчка про заблокированную «Напоминалку» и кнопка «Всё равно открыть».
5. Запустите приложение ещё раз и подтвердите.

Дальше оно будет открываться как обычное.

На macOS 14 и старше хватает правой кнопки по приложению → «Открыть» → «Открыть» в диалоге.

Если предпочитаете терминал, снять карантин можно одной командой:

```bash
xattr -dr com.apple.quarantine /Applications/Напоминалка.app
```

Если вместо предупреждения про разработчика macOS пишет, что приложение **повреждено**, значит скачана версия 0.2.0 — в ней была сломана подпись. Возьмите 0.2.1 или новее.

## Что умеет

- выбрать одного из четырёх питомцев: Винни, Макса, Совушку или Белочку;
- напоминать о воде, разминке и своих делах по интервалу, времени и дням недели;
- откладывать напоминание, если сейчас неудобно;
- не показывать уведомления в тихие часы;
- жить в трее и запускаться при входе в систему;
- запоминать громкость и положение всплывающего окна на экране;
- проигрывать реальные звуки питомцев;
- показывать плавные покадровые анимации на прозрачном фоне;
- хранить историю отмеченных пауз локально, без аккаунта и облака.

## Как это устроено

Приложение написано на Electron. У каждого питомца есть прозрачный WebP-атлас: пять анимаций, до 12 фаз движения в каждой. Всплывающее окно всегда поверх остальных, но его можно перетащить туда, где оно не мешает. Закрытие главного окна прячет приложение в трей, напоминания продолжают работать.

Настройки хранятся на компьютере пользователя. Никуда не отправляются ни история пауз, ни расписание, ни выбранный питомец.

## Запуск из исходников

Нужен Node.js 20 или новее.

```bash
npm install
npm start
```

Проверка текстов, склонений и сетки атласа:

```bash
npm test
```

## Сборка установщиков

```bash
npm run dist:mac
npm run dist:mac:x64
npm run dist:win
npm run dist:win:portable
```

Собранные файлы появятся в папке `dist`.

## Звуки и лицензии

Звуки взяты с Pixabay. Точные источники и условия указаны в [assets/sounds/SOURCES.md](assets/sounds/SOURCES.md).

## Разработка

Приложение сделано командой [Best Practice AI](https://bestpracticeai.ru/).
